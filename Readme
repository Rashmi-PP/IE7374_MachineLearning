The project's aim is to model classification algorithm that can predict if a customer would subscribe to a term deposit. A term deposit is a deposit made for a fixed duration normally for a fixed interest rate. Source (.csv) and code (.ipynb) files are included.
Methods used:
i. Logistic regression
ii. Support vector machine
iii. Feed forward neural network using Keras Classifier
